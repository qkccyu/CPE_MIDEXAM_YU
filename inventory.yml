ubuntu_servers:
  hosts:
    ubuntu_server1:
      ansible_host: 10.0.2.15

centos_servers:
  hosts:
    centos_server1:
      ansible_host: 192.168.122.1

influxdb_servers:
  hosts:
    influxdb_server:
<10.0.2.15>

grafana_servers:
  hosts:
    grafana_server:
      ansible_host: <10.0.2.15>

prometheus_servers:
  hosts:
    prometheus_server:
      ansible_host:
<10.02.15>
 
